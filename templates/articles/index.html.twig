{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .categories-item { list-style-type: none }
</style>

<div class="example-wrapper">
    <h1>Articles</h1>

    <p>Статьи доступны по адресу <code>/articles</code> методом <code>POST</code>, где <code>category</code> = id категории</p>

    {% if categories is not empty %}
        <h4>Все категории</h4>
        <ul>
            {% for category in categories %}
                <li class="categories-item">
                    <ul>
                        <li>id: {{ category.id }}</li>
                        <li>name: {{ category.name }}</li>
                    </ul>
                    {% if not loop.last %}
                        <hr>
                    {% endif %}
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>Категории не найдены. Дамп базы <code>articles.sql</code> в корне проекта</p>
    {% endif %}

</div>
{% endblock %}
